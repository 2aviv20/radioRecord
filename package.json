{
  "name": "100fmrecorder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pm2 start",
    "build": "npm install && tsc",
    "dev": "pm2 kill && tsc && pm2 start",
    "prod": "pm2 kill && git pull && tsc && pm2 start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.7.1",
    "express": "^4.16.4",
    "moment": "^2.29.2",
    "node-schedule": "^1.3.2",
    "node-telegram-bot-api": "^0.30.0",
    "request": "^2.88.0",
    "typescript": "^3.4.5"
  }
}
